'use strict'

const C = (function () {

  const CANVAS_PADDING = 50

  const SPHERE = {}
  SPHERE.DIAMETER = 400
  SPHERE.RADIUS = SPHERE.DIAMETER / 2
  SPHERE.BACKGROUND = {
    CX: SPHERE.RADIUS + CANVAS_PADDING / 2,
    CY: SPHERE.RADIUS + CANVAS_PADDING / 2,
    R: SPHERE.RADIUS,
    COLOR: 'black',
  }

  const PROJECTION = {}
  PROJECTION.SCALE = SPHERE.RADIUS
  PROJECTION.ROT_INIT = [0, 0, 0]
  PROJECTION.CLIP_ANGLE = 90 // deg
  PROJECTION.ORIGIN = {
    X: SPHERE.RADIUS + CANVAS_PADDING / 2,
    Y: SPHERE.RADIUS + CANVAS_PADDING / 2,
  }

  const CANVAS = {}
  CANVAS.PADDING = CANVAS_PADDING
  CANVAS.WIDTH = SPHERE.DIAMETER + CANVAS_PADDING
  CANVAS.HEIGHT = SPHERE.DIAMETER + CANVAS_PADDING

  const DATA = {}
  DATA.MAX_DATA_POINTS = 2000

  return {
    SPHERE: SPHERE,
    CANVAS: CANVAS,
    PROJECTION: PROJECTION,
    DATA: DATA,
  }
})()
